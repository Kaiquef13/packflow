// Este arquivo será gerado automaticamente pelo Amplify CLI
// Execute: amplify init && amplify add api && amplify push
const awsmobile = {
    "aws_project_region": "us-east-1",
};

export default awsmobile;
